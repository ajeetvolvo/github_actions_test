name: Main Workflow

on:
  push:
    branches:
      - master

jobs:

  hello-india:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Say Hello to India
        run: echo "Hello India!"

      - name: Deploy
        uses: ajeetvolvo/github-actions-test/.github/templates/action.yml@master
