name: Main Workflow

on:
  push:
    branches:
      - master

jobs:

  hello-india:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Say Hello to India
        run: echo "Hello India!"

      - name: Deploy
        run: "./.github/template/deploy/action.yml"
