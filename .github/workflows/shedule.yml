name: Print Hello World

on:
  schedule:
    - cron: "*/5 * * * *" # Runs every 5 minutes

jobs:
  hello_world:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello World
        run: echo "Hello, world!"
